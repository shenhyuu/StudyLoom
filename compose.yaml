services:
  studyloom:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - studyloom-data:/app/backend/data
      - ./backend/static:/app/backend/static:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
volumes:
  studyloom-data:
